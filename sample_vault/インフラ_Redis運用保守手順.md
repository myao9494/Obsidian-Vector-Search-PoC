# インフラ Redis 運用保守手順書

## 概要
本番環境における Redis クラスタのメモリ管理、バックアップ、障害復旧手順について定めます。

## 運用ポリシー
- メモリ上限（maxmemory）は物理メモリの70%に設定し、超過時は volatile-lru でエビクションします。
- スナップショット（RDB）は毎日午前3時に自動生成し、S3へ退避します。
- キャッシュフラッシュが必要な場合は `FLUSHDB ASYNC` コマンドを使用してください。
